{
  "version": 2,
  "installCommand": "npm install && cd LORA && npm install && cd ../SIMPUL/FE && npm install && cd ../web && npm install",
  "buildCommand": "cd web && npm run build",
  "outputDirectory": "web/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
