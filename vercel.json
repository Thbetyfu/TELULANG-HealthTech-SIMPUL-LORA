{
  "version": 2,
  "installCommand": "npm install --prefix LORA && npm install --prefix SIMPUL/FE && npm install --prefix web",
  "buildCommand": "npm run build --prefix web",
  "outputDirectory": "web/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
